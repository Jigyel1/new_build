<div class="email-container">
    <div class="email-content p-24">
        <div class="logo">
          <%= image_tag(attachments['logo.png'].url) %>
        </div>
        <h1 class="font-size--50">New Build Invitation</h1>
        <p class="color--primary"><%= t("devise.mailer.invitation_instructions.hello_user", lastname: @resource.lastname) %></p>

        <p class="line-height--22">
            <%= t('devise.mailer.invitation_instructions.description') %>
        </p>

        <div class="text-align--center my-32">
          <a href="<%= root_url %>">
            <button type="button" class="button button__primary"><%= t('devise.mailer.invitation_instructions.button_text') %></button>
          </a>
        </div>

        <p class="line-height--22">
        <% if @resource.invitation_due_at %>
          <%= t(
            "devise.mailer.invitation_instructions.accept_until",
            due_date: l(
              @resource.invitation_due_at,
              format: :'devise.mailer.invitation_instructions.accept_until_format'
            )
          ) %>
        <% end %>
        </p>
        <p class="line-height--22">
          <%= t('devise.mailer.trouble_text') %>
        </p>

        <p><%= t('devise.mailer.signature') %></p>
    </div>
    <div class="email-footer line-height--22 p-24">
      <p>
        This message was sent to
        <span class="color--primary">finch123@email.com</span>.
         If you have questions or not the intended recipient for this email, please ignore it or contact us.
      </p>
      <div class="mt-32">
        <a href="" class="color--primary font-size--18">Terms of use</a>
        <a href="" class="color--primary font-size--18 border--left-right mx-16 px-16">About Us</a>
        <a href="" class="color--primary font-size--18">Contact Us</a>
      </div>
      <p class="line-height--22 mt-24 color--dove-gray">

      ©<%= Date.current.year %> UPC. Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed quis odio proin sagittis pha tra dui vitae.
      </p>
    </div>
</div>
