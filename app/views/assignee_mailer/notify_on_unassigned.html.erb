<table class="full--width p-32">
  <tr>
    <td>
      <div class="logo mb-32">
        <%= image_tag(attachments['logo.png'].url) %>
      </div>
    </td>
  </tr>
  <tr>
    <td>
      <h1 class="m-0">New Build - Unassigned from Project</h1>
    </td>
  </tr>
  <tr>
    <td><p class="color--primary mb-16 font--bold titleCase">Hello <%= @user.profile.salutation %>. <%= @user.lastname.capitalize! %></p></td>
  </tr>
  <tr>
    <td>
      <p class="m-0">You have been unassigned as the Assigned Person for <%= @project.name %> project by <%= @current_user.name %>.
      </p>
    </td>
  </tr>
</table>
<%= render('shared/footer') %>